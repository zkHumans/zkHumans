{
  "name": "@zkhumans/contracts",
  "version": "0.0.1",
  "keywords": [
    "mina-zkapp",
    "mina-zk-app",
    "mina-dapp",
    "zkapp"
  ],
  "type": "module",
  "scripts": {
    "build": "ln -sf ../../node_modules . && mkdir -p node_modules/@zkhumans && ln -sf ../../dist/libs/snarky-bioauth ./node_modules/@zkhumans/ && cd ../../ && nx run build --project=contracts-zkhumans --configuration=production --skip-nx-cache --clean=false"
  }
}
